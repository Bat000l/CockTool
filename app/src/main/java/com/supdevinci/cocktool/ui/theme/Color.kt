package com.supdevinci.cocktool.ui.theme

import androidx.compose.ui.graphics.Color

// 🍹 PALETTE CHAUDE SIMPLIFIÉE - Inspiration Cocktail Bar
// Couleurs chaudes basées sur Orange/Amber/Rust

// Primaire: Orange Chaud (dominant)
val WarmOrange40 = Color(0xFFE85D2D)         // Orange chaud foncé
val WarmOrange80 = Color(0xFFFFB88C)         // Orange chaud clair

// Secondaire: Rust/Terracotta (complement chaud)
val WarmRust40 = Color(0xFFB84520)           // Rust/Terracotta foncé
val WarmRust80 = Color(0xFFD48C6A)           // Rust/Terracotta clair

// Accents
val DarkBackground = Color(0xFF1A1410)       // Fond très sombre (chocolat foncé)
val DarkSurface = Color(0xFF2D2420)          // Surface sombre (brun foncé)
val LightBackground = Color(0xFFFEF6F1)      // Fond très clair (crème)
val LightSurface = Color(0xFFFFF0E6)         // Surface clair (orange très pâle)

// Textes
val TextDark = Color(0xFF1A1410)             // Texte sombre
val TextLight = Color(0xFFFFF0E6)             // Texte clair
